name: 'Check, scripts'
on:
  push:
    branches:
      - master

jobs:
  check_scripts:
    name: Check scripts
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@master
      - name: Install Shellcheck
        run: sudo apt-get install shellcheck
      - name: Run Shellcheck
        run: make check
        shell: bash
      - name: Run Installation Script
        run: 'yes | python3 install.py'
        shell: bash
      - name: Run Installation Script Twice
        run: |
          python3 install.py
          python3 install.py
        shell: bash
